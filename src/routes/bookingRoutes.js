const express = require('express');
const router = express.Router();

// Booking routes are handled under /customer routes
// See src/routes/customerRoutes.js for booking endpoints

module.exports = router;
